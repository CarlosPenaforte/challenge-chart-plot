/*import { render, screen } from '@testing-library/react';
import App from './App';

// Under development...

test("Renders a header with a title", () => {
  render(<App />);

  const HEADER = screen.getByRole("header");

  expect(HEADER).toBeInTheDocument();
});

test("Renders an input code editor", async () => {
  render(<App />);

  const CODE_INPUT = await screen.findByPlaceholderText(/Please/i);

  expect(CODE_INPUT).toBeInTheDocument();
});

test("Renders a line chart", async () => {
  render(<App />);

  const CHART = await screen.findByText(/Waiting/i);

  expect(CHART).toBeInTheDocument();
});

test("Renders a footer with a button", () => {
  render(<App />);

  const FOOTER = screen.getByRole("footer");
  const BUTTON = screen.getByRole("button");

  expect(FOOTER).toBeInTheDocument();
  expect(BUTTON).toBeInTheDocument();
});*/